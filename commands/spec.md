---
description: Interview and write a spec for a change to this codebase, then list the policies it touches.
argument-hint: <what you want to change>
---

Use the `spec` skill from the Brownspec plugin for this change:

$ARGUMENTS

Read the repository before asking anything. Six questions or fewer. Rewrite the spec on
disk after every answer. Run policy routing as a separate pass once the spec is otherwise
finished.
