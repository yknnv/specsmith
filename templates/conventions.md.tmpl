# Conventions

**Generated by `/specsmith:init` — this file is overwritten on every run.**
Do not hand-edit rules you want to keep. If a rule is a decision rather than an
observation, it belongs in `policies.md`, which is never overwritten.

This file describes **what this codebase actually does today**, not what it should do.
Where the code is inconsistent, both variants are recorded.

**Generated:** <date> · **From:** <modules sampled>

---

## Errors

- <rule>

  ```<lang>
  <two-line excerpt from the repository>
  ```
  _Seen in: `<path>`_

## Logging

## Layers

What calls what, and what is not allowed to call what.

## Naming

Modules, functions, tests, database objects.

## Tests

Levels that exist, what a change is expected to come with, how fixtures work.

## Data and migrations

How migrations are written, and whether they are reversible in practice.

## Configuration and secrets

---

## Inconsistencies

Where the codebase contradicts itself. Both variants, with where each lives. Not
averaged into a rule that is true nowhere.

| Topic | Variant A | Variant B | Where each dominates |
| --- | --- | --- | --- |
| | | | |
